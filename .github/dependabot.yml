---
version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (and resolves Node runtime
  # deprecations as action maintainers bump their runtimes).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns: ["*"]
